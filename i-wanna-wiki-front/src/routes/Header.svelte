<script>
	import { page } from '$app/stores';
	import logo from '$lib/images/wiki_logo.webp';
</script>

<header class="bg-slate-800">
	<div class="ml-3 corner">
		<a href="https://www.iwannawiki.com">
			<img src={logo} alt="I wanna wiki" />
			<p class="ml-2 text-white">I wanna wiki</p>
		</a>
	</div>

	<nav>
		<ul>
			<li aria-current={$page.url.pathname === '/' ? 'page' : undefined}>
				<a href="/">Home</a>
			</li>
			<!-- <li aria-current={$page.url.pathname === '/submit' ? 'page' : undefined}>
				<a href="/submit">Submit</a>
			</li>
			<li aria-current={$page.url.pathname === '/game' ? 'page' : undefined}>
				<a href="/game">List</a>
			</li>
			<li aria-current={$page.url.pathname === '/game/random' ? 'page' : undefined}>
				<a href="/game/random">Random</a>
			</li>
			<li aria-current={$page.url.pathname === '/game/search' ? 'page' : undefined}>
				<a href="/game/search">Search</a>
			</li> -->
		</ul>
	</nav>

	<div class="corner" />
</header>

<style>
	header {
		font-family: 'Comic Sans Pro';
		display: flex;
		justify-content: space-between;
	}

	.corner {
		width: 10em;
		height: 3em;
	}

	.corner a {
		display: flex;
		align-items: center;
		justify-content: center;
		width: 100%;
		height: 100%;
	}

	.corner img {
		width: 2em;
		height: 2em;
		object-fit: contain;
	}

	nav {
		display: flex;
		justify-content: center;
		--background: rgba(255, 255, 255, 0.7);
	}

	ul {
		position: relative;
		padding: 0;
		margin: 0;
		height: 3em;
		display: flex;
		justify-content: center;
		align-items: center;
		list-style: none;
		background: var(--background);
		background-size: contain;
	}

	li {
		position: relative;
		height: 100%;
	}

	li[aria-current='page']::before {
		--size: 6px;
		content: '';
		width: 0;
		height: 0;
		position: absolute;
		top: 0;
		left: calc(50% - var(--size));
		border: var(--size) solid transparent;
		border-top: var(--size) solid var(--color-theme-1);
	}

	nav a {
		display: flex;
		height: 100%;
		align-items: center;
		padding: 0 0.5rem;
		color: var(--color-text);
		font-weight: 700;
		font-size: 0.8rem;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		text-decoration: none;
		transition: color 0.2s linear;
	}

	a:hover {
		color: var(--color-theme-1);
	}
</style>
